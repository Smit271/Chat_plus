<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/empty"
        android:title=""
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/menu_icon">

        <menu>
            <item android:id="@+id/action_chat"
                android:icon="@drawable/chat_icon"
                app:showAsAction="ifRoom"
                android:title="Chats" />

            <item android:id="@+id/action_add_friend"
                android:icon="@drawable/add_friends_icon"
                app:showAsAction="ifRoom"
                android:title="Add Friends" />

            <item android:id="@+id/action_pending_requests"
                android:icon="@drawable/pending_request_icon"
                app:showAsAction="ifRoom"
                android:title="Pending Requests" />

            <item android:id="@+id/action_logout"
                android:icon="@drawable/logout_icon"
                app:showAsAction="ifRoom"
                android:title="Logout" />

        </menu>

    </item>

</menu>